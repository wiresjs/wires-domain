var _ = require('lodash');
// Register local services
module.exports = function(domain)
{	
	/*var required = function(){
		_.each(arguments, function(item){
			console.log(item);
		});
	}

	// Register $body for convinience usage
	domain.register("$body",function($req){
		var $body = {}
		$body.required = required.bind($req.body);

		return $body
	})

	// This services will validate and process body and query 
	domain.register("$required",function($req, $body){

		return function(){
			
		}
	});*/
}